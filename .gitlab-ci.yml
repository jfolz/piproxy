stages:
  - build

rust:
  stage: build
  image: rust:latest
  script:
    - apt-get update
    - apt-get install -y cmake
    - cargo build --verbose --release
    - cargo test --verbose --release
  artifacts:
    paths:
    - target/release/piproxy
